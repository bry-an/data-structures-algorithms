class TwoDIterator {
    constructor(array) {
        this.array = array;
        this.outerPointer = 0;
        this.innerPointer = 0;
    }

    next() {
        if (!this.array[this.outerPointer][this.innerPointer]) {
            
        }

    }
}